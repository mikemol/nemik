[project]
name = "nemik"
version = "0.0.0"
description = "Standards-first project management over federated agent workstreams: OSLC state, ForgeFed messages."
requires-python = ">=3.13"
dependencies = [
    "rdflib>=7",
    "pyshacl>=0.26",
    "mikemol-pathsforward @ git+https://github.com/mikemol/mtools.git@2293751f035ff1874da1b298d5cb82e968ee9ff5#subdirectory=pathsforward",
]

[project.scripts]
nemik-check = "nemik.check:main"
nemik-metrics = "nemik.metrics:main"
nemik-serve = "nemik.serve:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.metadata]
allow-direct-references = true
